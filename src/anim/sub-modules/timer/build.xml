<project name="anim/timer.build" default="build" basedir=".">
    <description>anim/timer Build File</description>

    <property name="component.name" value="anim/timer"/>

    <import file="../../../common.xml"/>

    <target name="build" depends="common-build.build">
    </target>
</project>
