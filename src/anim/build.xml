<project name="anim.build" default="build" basedir=".">
    <description>Anim Build File</description>

    <property name="component.name" value="anim"/>

    <import file="../../tools/build-xml/common.xml"/>

    <target name="build" depends="common-build.build">
    </target>
</project>
