<project name="htmlparser.build" default="build" basedir=".">
    <description>HtmlParser Build File</description>

    <property name="component.name" value="htmlparser"/>

    <import file="../common.xml"/>

    <target name="build" depends="common-build.build">
    </target>
</project>
