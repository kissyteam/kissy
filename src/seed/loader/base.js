/**
 * setup data structure for kissy loader
 * @author yiminghe@gmail.com
 */
(function(S){
    if("require" in this) return;
    S.__loader={};
    S.__loaderUtils={};
    S.__loaderData={};
})(KISSY);