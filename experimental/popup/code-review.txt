
 - add("datagrid")
 - S.Event ,

 - trigger 始终是 [], !trigger 应该判断 triggger.length
 - getXY 已经考虑相对定位？ ready 里 appendChild 没必要？
 - config || {} 没必要
 - Popup.Config 还是 var defaultConfig ?
 
 
 - createEl ? ready ?
 - iframe shim. no S.ready! style 只需添加一次
 
 - self.trigger = [] ?

 - 建议 constructor 里只处理属性的初始化，事件相关的代码放到 _bindUI 里。或
   者按照卫语句 -> config -> initPopup -> initMask -> initShim 来组织

 - 默认配置：effect, closeBtnCls（有默认值会更好）, disableClick ?
 - mix -> augment
 - Popup 静态成员也可以用 S.mix(Popup, { })
 - createMask ?
 - Popup 上的方法，如果不需要 public，都可以直接变成私有变量
 - getIndexArrEl 用 S.indexOf(el, arr)

 - prototype 里，除了 show 和 hide，其它的都可以 protected
 - attatchTrigger 建议：_bindTrigger
 - setTrigger 里可以用 makeArray
 - 事件的参数


